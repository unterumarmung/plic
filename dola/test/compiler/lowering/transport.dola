// RUN: dola --emit-lowered-mlir %s | FileCheck %s
// CHECK: llvm.mlir.constant(38 : i32)
// CHECK: llvm.call @dola_rt_codegen
// CHECK-NOT: dola.
module test.lower_transport;
use std.transport;
fn main() { let listener = transport.listen[Int, Int]("127.0.0.1:0"); }
