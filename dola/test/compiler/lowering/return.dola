// RUN: dola --emit-mlir %s | FileCheck %s --check-prefix=HIGH
// RUN: dola --emit-lowered-mlir %s | FileCheck %s --check-prefix=LOW
// HIGH: cf.cond_br
// HIGH: return
// HIGH: return
// LOW-NOT: cf.
// LOW: llvm.cond_br
// LOW: llvm.return
module test.early_return;

fn main() -> Int {
    if true {
        return 0;
    } else {
        return 1;
    };
}
