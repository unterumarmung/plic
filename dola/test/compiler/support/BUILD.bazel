load("@rules_cc//cc:cc_test.bzl", "cc_test")

filegroup(
    name = "format_sources",
    srcs = glob(["*.cpp", "*.h"], allow_empty = True),
    visibility = ["//visibility:public"],
)

cc_test(
    name = "strong_id_test",
    srcs = ["strong_id_test.cpp"],
    deps = [
        "//compiler/sema",
        "//compiler/support",
        "@googletest//:gtest_main",
    ],
)
